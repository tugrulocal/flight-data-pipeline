apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumer
  template:
    metadata:
      labels:
        app: consumer
    spec:
      # Consumer başlangıçta MongoDB indexlerini oluşturur ve Kafka'ya bağlanır.
      # İki bağımlılık hazır olmadan ana süreci başlatmıyoruz.
      initContainers:
        - name: wait-for-kafka-and-mongodb
          image: apache/kafka-native:4.3.1@sha256:2885898ba17065023f1bd605f3a81efcfa986014f062b73b91ef5462485f9060
          command:
            - /bin/sh
            - -ec
            - |
              until nc -z -w 2 kafka 29092 && nc -z -w 2 mongodb 27017; do
                echo 'Kafka ve MongoDB bekleniyor...'
                sleep 2
              done
      containers:
        - name: consumer
          image: ghcr.io/tugrulocal/flight-data-pipeline-consumer@sha256:a722b7ab94dd67caafd094a1a950de672615824f4d0004265cace5a54014364e
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: consumer-config
