apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    # ServiceMonitor bu sabit etiketi kullanarak backend endpoint'ini bulur.
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
