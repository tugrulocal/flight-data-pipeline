apiVersion: v1
kind: Service
metadata:
  name: producer-metrics
  labels:
    # ServiceMonitor bu sabit etiketten producer endpoint'ini bulur.
    app: producer-metrics
spec:
  type: ClusterIP
  selector:
    app: producer
  ports:
    - name: metrics
      port: 8000
      targetPort: metrics
